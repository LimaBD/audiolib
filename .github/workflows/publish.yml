name: Publish

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      target:
        description: "Publish target (testpypi or pypi)"
        required: true
        default: testpypi
        type: choice
        options:
          - testpypi
          - pypi

permissions:
  contents: write   # for GitHub release
  id-token: write   # for OIDC trusted publishing

jobs:
  # -------------------------------------------------------------------------
  # 1. Build binary wheels for all targets
  # -------------------------------------------------------------------------
  build-wheels:
    name: Build wheel — ${{ matrix.target }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux x86_64
          - runner: ubuntu-latest
            target: x86_64
            manylinux: auto
          # Linux aarch64 (cross-compile)
          - runner: ubuntu-latest
            target: aarch64
            manylinux: auto
          # macOS Intel
          - runner: macos-14
            target: x86_64
            manylinux: ""
          # macOS Apple Silicon
          - runner: macos-14
            target: aarch64
            manylinux: ""
          # Windows x86_64
          - runner: windows-latest
            target: x86_64
            manylinux: ""

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Build wheels (Linux)
        if: runner.os == 'Linux'
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          manylinux: ${{ matrix.manylinux }}
          args: --release --out dist --locked --interpreter python3.10 python3.11 python3.12 python3.13
          sccache: "true"

      - name: Build wheels (macOS / Windows)
        if: runner.os != 'Linux'
        uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          args: --release --out dist --locked
          sccache: "true"

      - name: Upload wheel artifacts
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.runner }}-${{ matrix.target }}
          path: dist/

  # -------------------------------------------------------------------------
  # 2. Build source distribution
  # -------------------------------------------------------------------------
  build-sdist:
    name: Build sdist
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Build sdist
        uses: PyO3/maturin-action@v1
        with:
          command: sdist
          args: --out dist

      - name: Upload sdist artifact
        uses: actions/upload-artifact@v4
        with:
          name: sdist
          path: dist/

  # -------------------------------------------------------------------------
  # 3. Smoke-test the wheels
  # -------------------------------------------------------------------------
  test-wheels:
    name: Test wheel — Python ${{ matrix.python-version }} / ${{ matrix.os }}
    needs: [build-wheels, build-sdist]
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.10", "3.11", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Download all wheel artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: wheels-*
          path: dist/
          merge-multiple: true

      - name: Install test deps
        run: pip install pytest numpy soundfile scipy

      - name: Install wheel
        shell: bash
        run: pip install audiolib --find-links dist/ --no-deps

      - name: Run smoke tests
        run: pytest tests/ -v --ignore=tests/benchmark.py --ignore=tests/test_compat.py -x

  # -------------------------------------------------------------------------
  # 4. Publish to TestPyPI or PyPI
  # -------------------------------------------------------------------------
  publish:
    name: Publish to ${{ github.event.inputs.target || 'pypi' }}
    needs: test-wheels
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.event.inputs.target == 'testpypi' && 'testpypi' || 'pypi' }}
      url: ${{ github.event.inputs.target == 'testpypi' && 'https://test.pypi.org/p/audiolib' || 'https://pypi.org/p/audiolib' }}
    permissions:
      id-token: write  # OIDC trusted publishing — no stored secret needed

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: wheels-*
          path: dist/
          merge-multiple: true

      - name: Download sdist
        uses: actions/download-artifact@v4
        with:
          name: sdist
          path: dist/

      - name: Publish to TestPyPI
        if: github.event.inputs.target == 'testpypi'
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
          packages-dir: dist/

      - name: Publish to PyPI
        if: github.event.inputs.target != 'testpypi'
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/

  # -------------------------------------------------------------------------
  # 5. Create GitHub release
  # -------------------------------------------------------------------------
  github-release:
    name: GitHub Release
    needs: publish
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')

    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: wheels-*
          path: dist/
          merge-multiple: true

      - name: Download sdist
        uses: actions/download-artifact@v4
        with:
          name: sdist
          path: dist/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true
